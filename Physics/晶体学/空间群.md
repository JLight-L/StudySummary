# 记号
- 纯点式操作：
    - 旋转轴：n（n重转轴，n-fold）
    - 旋转反演轴：$\bar{n}$
        - 反射面（Mirror plane）：m = $\bar{2}$ 
- 点式操作+平移操作（可以做 $\left\{ R\mid \mathbf{t}\right\}=\left\{ E\mid \mathbf{b} \right\}\{R\mid \mathbf{t'}\}\left\{ E\mid -\mathbf{b} \right\}$ 分解，相当于改变点式操作的中心，使操作更直观）：
    - 滑移反射面（Glide plane）：旋转反演+平移。滑移轴平行于反射面；a,b,c（沿轴滑移），n（沿两个轴对角线滑移），e（双滑移，对于体心体系可能出现，即有两个滑移轴，均平行于反射面），g（二维体系，滑移反射轴）
        -  #？？？ 为什么没有3、4、6阶旋转反演和平移的组合？
    - 螺旋轴（Screw axis）：旋转+平移。$n_{p}$ ，n重转轴，平移向量为 $\frac{p}{n}\mathbf{R}_{0}$ 

# 空间群

## 平移操作与旋转操作
三维离散平移周期性：a, b, c 三个方向的平移

在具有平移周期性的情况下，旋转操作只能有 **1, 2, 3, 4, 6** 这几种可能

 $\forall \left\{ R\mid \mathbf{t} \right\}$ ：
 - 转动部分一定满足 $R\mathbf{R}_{n}=\mathbf{R}_{m}$ 
 - 平移部分满足：
     1. $\mathbf{t}=\mathbf{R}_{m}$ ，对应点式（symmorphic）操作，对应的旋转操作直接存在于空间群
     2. $\mathbf{t}=\mathbf{R}_{m}+\frac{L}{n}\mathbf{R}_{0}$ ，$L<n$ ，$R_{0}$ 为 $n$ 次转轴方向的最小晶格矢量（一定有晶格格矢平行该方向），对应非点式操作 #Really？ 滑移反射并不满足，这时t垂直于转轴（反射面和t平行）；对于不包含反演的旋转操作，通过坐标选取可以去除垂直转轴的平移分量，但对于包含反演的操作，通过坐标选取可以去除平行转轴的平移分量。

所有旋转操作构成点群 $G_{0}$ ，称为空间群的点群；因此标记空间群的一种办法：点群+格子（代表平移操作和旋转操作的组合方式）

# 亚周期群

#？？？ 为什么需要亚周期群？
#？？？ 如何给亚周期群全部命名？

以*层群*为例：
- 层群一定是某个空间群的子群，可能是某多个空间群的子群，因此若通过空间群寻找只有二维平移的子群，需要降重
- 对某个空间群找其可能的层群子群：取出两个晶格平移操作，即：
    1. 删去包含平面外平移的操作——对应二维平移对称性
    2. 删去转轴不垂直平面的转轴（平行平面的二次轴除外）——对应二维平移对称性（对应另一个方向的非周期性对称性）
- 降重：对于所有由空间群产生的层群，寻找所有等价的群
    1. 两个空间群生成来的两个层群：由于一个层群一定可以在第三个方向新增一个垂直平面的平移轴，故**只需要分析满足如下条件的群**：
        1. 最多只包含一个高阶轴，且为纯旋转（反演）轴（通常为 c 方向）；选取最高阶的纯旋转轴方向为额外方向（可能为二阶或一阶（任意方向））
            - 一定有垂直该的二维平移对称性
            - 其它二阶对称性一定垂直该轴
    2. 

## 分类
The subperiodic groups are classified into *affine subperiodic  
group types* (i.e. affine equivalence classes of subperiodic groups) or *proper affine  
subperiodic group types* (i.e. proper affine classes of subperiodic  
groups) #？？？ 

### 仿射变换
保持直线和平行性质不变的变换。线性变换和平移的组合。

### 欧几里得变换
保持距离（等距）的仿射变换

- *Question* ：为什么空间群中的操作都是等距操作？
    - *Answer* ：因为大部分情况下等距操作才有可能保持所有原子位点不变